---
name: paper-pipeline
description: 论文写作核心流水线——从主题确认到终稿修改的全流程
---

When invoked, read `${CLAUDE_PLUGIN_ROOT}/agents/paper-pipeline.md` first.
Treat that file as the governing workflow.
If running outside plugin mode (e.g. permanent install), read `~/paper-cowork-plugin/agents/paper-pipeline.md` instead.
