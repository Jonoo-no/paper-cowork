---
name: setup-paper-workspace
description: 一次性初始化论文工作区——创建目录结构、放入模板、检测依赖、引导准备素材
---

When invoked, read `${CLAUDE_PLUGIN_ROOT}/agents/setup-paper-workspace.md` first.
Treat that file as the governing workflow.
If running outside plugin mode (e.g. permanent install), read `~/paper-cowork-plugin/agents/setup-paper-workspace.md` instead.
